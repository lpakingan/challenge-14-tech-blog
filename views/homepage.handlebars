<section class="homepage-page">

    <div class="container display-flex">
        <div class="box col-12">
            {{#each posts as |post|}}
            <a href="./post/{{id}}">
            <div class="card flex-column box-shadow">
                <div class="card-header display-flex justify-space-between">
                    <h2> {{post.post_title}}</h2>
                    <p class="my-auto">Posted by {{user.username}} on {{format_date post.post_date}}</p>
                </div>
                <div class="card-body bg-light" style="flex: 1; color:black">
                    <p>{{post.post_content}}</p>
                </div>
            </div>
            </a>
            {{/each}}
        </div>
    </div>
    
</section>
