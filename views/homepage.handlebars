<div class="container">
    <div class="row">
        {{#each posts as |post|}}
        <a href="./blogpost/{{id}}">
        <div class="card row">
            <div class="card-header col-md-6">
                <h2> {{post.post_title}}</h2>
                <h3>Posted by {{post.user_id}} on {{format_date post.post_date}}</h3>
            </div>
            <div class="card-body">
                <p>{{post.post_content}}</p>
            </div>
        </div>
        </a>
        {{/each}}
    </div>
</div>
